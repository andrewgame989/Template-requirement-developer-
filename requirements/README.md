# Folder Requirement Aktif

Simpan requirement yang sudah diisi di sini dengan penamaan:

```
REQ-XXX-nama-fitur.md
```

Salin dari `templates/requirement-template.md` (lengkap) atau `templates/requirement-template-lite.md` (ringkas).
